// Том цели проекта Seed.
// Определяет желаемое конечное состояние системы как фиксированную точку.
// Фундаментальные принципы и характеристики результата, к которому мы стремимся.

#using namespace KQM(file: "core/KQM.tome")

namespace goal {

  seed_end_state: @Statement(inf) {
    Core:
      Seed должен завершиться имея состояние переносимого зерна для старта проектов.
    Reason:
      Факт появился как результат осознания необходимости фиксированной цели для проекта Seed.
  }
}